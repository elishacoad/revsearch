export const Decision = Object.freeze({
    NONE: "none",
    INCLUDE: "include",
    MAYBE: "maybe",
    EXCLUDE: "exclude"
});

export const Colors = Object.freeze({
    "include": "#83ddb2",
    "maybe": "#cde9ff",
    "exclude": "#eeab9e"
});